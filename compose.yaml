name: gaggle

# One-shot local dev stack: Postgres + Redis + API + Web (nginx).
#   make dev          # or: docker compose up --build -d
#   make dev-stop     # or: docker compose down
# The API container auto-applies migrations on start (idempotent) and the
# seed users are created with `make seed`.

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-white}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-teeth}
      POSTGRES_DB: ${DB_NAME:-social}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${DB_PORT:-6969}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-white} -d ${DB_NAME:-social}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    image: gaggle-api
    build:
      context: ./server
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      DB_ADDRESS: db:5432
      DB_USER: ${DB_USER:-white}
      DB_PASSWORD: ${DB_PASSWORD:-teeth}
      DB_NAME: ${DB_NAME:-social}
      DB_MAX_OPEN_CONNS: 30
      DB_MAX_IDLE_CONNS: 15
      DB_MAX_IDLE_TIME: 60
      DB_QUERY_TIMEOUT_SECONDS: 5
      REDIS_ADDRESS: redis:6379
      REDIS_DB: 0
      REDIS_FEED_CACHE_TTL_SECONDS: 60
      RATE_LIMIT_MAX_REQUESTS: 30
      RATE_LIMIT_WINDOW_SECONDS: 60
      SERVER_ADDR: ":2021"
      SERVER_WRITE_TIMEOUT: 30
      SERVER_READ_TIMEOUT: 10
      SERVER_IDLE_TIMEOUT: 360
      SERVER_REQUEST_TIMEOUT: 2
      MEDIA_DIR: /app/media
      LOGGING_FILENAME: /app/logs/logs.log
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-me}
      JWT_ISSUER: gaggle
      JWT_ACCESS_TOKEN_EXPIRATION_TIME: 900
      JWT_REFRESH_TOKEN_EXPIRATION_TIME: 1296000
      COOKIE_SECURE: "false"
      INVITATION_DURATION: 86400
      # Google OAuth (optional; when unset the /auth/google endpoints return 501 and the frontend hides the button)
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URL: ${GOOGLE_REDIRECT_URL:-}
      GOOGLE_FRONTEND_REDIRECT_URL: ${GOOGLE_FRONTEND_REDIRECT_URL:-http://localhost:5173}
      GOOGLE_ALLOW_UNVERIFIED_EMAIL: ${GOOGLE_ALLOW_UNVERIFIED_EMAIL:-false}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:5173}
      # Used by the entrypoint to apply migrations on first start.
      POSTGRES_URL: postgres://${DB_USER:-white}:${DB_PASSWORD:-teeth}@db:5432/${DB_NAME:-social}?sslmode=disable
      MIGRATE_ON_START: "true"
      SEED_ON_START: "${SEED_ON_START:-true}"
    ports:
      - "${API_PORT:-2021}:2021"
    volumes:
      - api_media:/app/media
      # Host filesystem mounted read-only so the admin metrics endpoint can
      # report real disk usage via statfs (the internal/metrics package falls
      # back to "/" when this mount is absent, e.g. in tests).
      - /:/host:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:2021/swagger/doc.json >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  web:
    image: gaggle-web
    build:
      context: ./web
      args:
        VITE_API_BASE_URL: /api/v1
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      # Host port for the frontend (local dev default 5173). Production sets
      # WEB_PORT=80 / WEB_HTTPS_PORT=443 via .env (see deploy/apply.sh). HTTPS
      # is always listened on 443 *inside* the container (web/docker-entrypoint.sh
      # + nginx.conf); publishing the host port is opt-in per environment.
      - "${WEB_PORT:-5173}:80"
      - "${WEB_HTTPS_PORT:-}:443"
    volumes:
      # Holds the TLS cert (self-signed fallback and/or the real certbot
      # cert) and the certbot ACME webroot, so the 443 listener always has
      # valid files. The letsencrypt volume must be mounted at the SAME path
      # (/etc/letsencrypt) in web and certbot — certbot's live/gaggle entries
      # are symlinks into archive/, so nginx needs the whole tree.
      - letsencrypt:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    healthcheck:
      # nginx serves the SPA and reverse-proxies /swagger/ to the api — the
      # healthcheck rides that proxied path so "web healthy" means the whole
      # request path the deploy probes is actually serving, not just that
      # nginx bound a socket. It ALSO asserts the fixed-name public assets
      # (favicon.ico, gaggle-goose.png) return 200: these keep FIXED filenames
      # while /assets/* are content-hashed, and the SPA fallback would 200 a
      # MISSING file, so a broken dist layer surfaces only as nginx 403 —
      # probing them here makes a bad web image fail `up --wait` instead of
      # sailing through to the deploy's own gate.
      # TLS itself is guaranteed: the entrypoint either mounts a real cert or
      # generates the self-signed fallback before nginx boots, so nginx
      # refusing to start is caught here via HTTPS.
      # Use 127.0.0.1, not localhost: nginx binds IPv4 only and "localhost"
      # resolves to ::1 inside the container (Docker /etc/hosts), which busybox
      # wget can't reach → a false-unhealthy check. busybox wget exits non-zero
      # on HTTP errors (403/404), which is what flips the health state.
      test: ["CMD-SHELL", "wget -qO- --no-check-certificate https://127.0.0.1/swagger/doc.json >/dev/null 2>&1 && wget -qO- --no-check-certificate https://127.0.0.1/favicon.ico >/dev/null 2>&1 && wget -qO- --no-check-certificate https://127.0.0.1/gaggle-goose.png >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  # Local Go toolchain without needing anything installed on the host.
  # Not started by `make dev` (profile "tools"); use it via:
  #   docker compose --profile tools run --rm tools <go command>
  # e.g. `make test`, `make swag`.
  tools:
    image: golang:1.24-alpine
    profiles: ["tools"]
    working_dir: /src
    volumes:
      - ./server:/src
      - go_mod_cache:/go/pkg/mod
    environment:
      DB_USER: ${DB_USER:-white}
      DB_PASSWORD: ${DB_PASSWORD:-teeth}
      DB_NAME: ${DB_NAME:-social}
      DB_ADDRESS: db:5432
      REDIS_ADDRESS: redis:6379
      # Test helper talks to the real Postgres on a throwaway `social_test` DB.
      TEST_DB_ADDRESS: db:5432
      TEST_DB_USER: ${DB_USER:-white}
      TEST_DB_PASSWORD: ${DB_PASSWORD:-teeth}
    depends_on:
      db:
        condition: service_healthy

  # Node toolchain for frontend lint/build without installing node on the host.
  web-tools:
    image: node:24-alpine
    profiles: ["tools"]
    working_dir: /src
    volumes:
      - ./web:/src
      - npm_cache:/root/.npm

volumes:
  postgres_data:
  redis_data:
  api_media:
  go_mod_cache:
    name: go_mod_cache
  npm_cache:
    name: npm_cache
  letsencrypt:
  certbot_www:
